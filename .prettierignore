dist
coverage
playwright-report
test-results
package-lock.json

# Generated by npm run db:types
src/adapters/supabase/database.types.ts

# Generated by npm run api:types
src/adapters/rest/api.types.ts

# The spec as the owner pasted it, kept byte for byte so the next version can be diffed against it.
docs/spec.md

# The Symfony server: PHP tooling formats it, not Prettier.
api

# Claude Code's session worktrees: whole other checkouts of this repository, which git excludes through
# .git/info/exclude where neither Prettier nor ESLint looks. Linting them lints someone else's branch.
.claude/worktrees
